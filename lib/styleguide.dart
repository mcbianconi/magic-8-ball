import 'package:flutter/material.dart';

class AppColors {
  static Color primary = Colors.white;
  static Color secondary = Colors.pinkAccent;
  static Color background = Color(0xffca035c);
}

TextStyle header = TextStyle(fontSize: 22, color: AppColors.primary);

TextStyle content = TextStyle(fontSize: 18, color: AppColors.primary);
